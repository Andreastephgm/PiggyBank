package com.miempresa.findapp03;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

public class EditarBanco extends AppCompatActivity {
/*
    EditText etNombre, etSurcursal, etTipo, etDireccion, etHorario;
    Button editar;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_agregar_banco);
        etNombre = findViewById(R.id.etnombrein);
        etSurcursal = findViewById(R.id.etsurcursalin);
        etTipo = findViewById(R.id.ettipoin);
        etDireccion = findViewById(R.id.etdireccionin);
        etHorario = findViewById(R.id.ethorarioin);
        editar = findViewById(R.id.updateProfileButton);

        Bundle extras = getIntent().getExtras();
        String nombre = extras.getString("nombre");
        String surcursal = extras.getString("surcursal");
        String tipo = extras.getString("tipo");
        String direccion = extras.getString("direccion");
        String horario = extras.getString("horario");


        etNombre.setText(nombre);
        etSurcursal.setText(surcursal);
        etTipo.setText(tipo);
        etDireccion.setText(direccion);
        etHorario.setText(horario);


        editar.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                editarBanco();
            }
        });

    }


    public int editarBanco(){
        return 1;
    }
    
 */
}